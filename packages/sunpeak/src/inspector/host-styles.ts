/**
 * Re-export default style variables from the shared module.
 * Host shells import from here; the canonical definition lives in lib/.
 */
export { DEFAULT_STYLE_VARIABLES } from '../lib/default-style-variables';
