---
title: "Lifecycle"
description: "How the CLI fits into your development workflow."
keywords: ["MCP Apps", "MCP app framework", "CLI", "development workflow", "ChatGPT Apps"]
---

## Overview

The sunpeak CLI commands support a complete development lifecycle, from project creation to production builds. This page explains how the commands work together.

## Development Lifecycle

```mermaid
flowchart LR
    A[sunpeak new] --> B[sunpeak dev]
    B --> C[sunpeak build]
    C --> D[sunpeak start]
```

### 1. Create

Install sunpeak globally:

<Tabs>
  <Tab title="pnpm">
    ```bash
    pnpm add -g sunpeak
    ```
  </Tab>
  <Tab title="npm">
    ```bash
    npm install -g sunpeak
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn global add sunpeak
    ```
  </Tab>
</Tabs>

Then start a new project:

```bash
sunpeak new
```

This scaffolds a complete sunpeak project with all the configuration and structure you need.

### 2. Develop

Run the development server with hot reload:

```bash
sunpeak dev
```

This starts:
- The simulator at `http://localhost:3000` for local development
- An MCP server at `http://localhost:8000` for testing with real MCP clients
- A sandbox proxy for cross-origin iframe isolation (matching production host behavior)

Both the simulator and MCP server support Hot Module Reload, letting you iterate quickly on your App UI.

### 3. Build

Create production-ready bundles:

```bash
sunpeak build
```

This compiles and optimizes your Resources, Tools, and optional server entry for production.

### 4. Start

Start the production MCP server:

```bash
sunpeak start
```

This loads your compiled tools (with real handlers and Zod schemas), serves pre-built resource HTML, and runs your `auth()` function if present. See the [Deployment Guide](/guides/deployment) for details on publishing to ChatGPT.

## Dive Deeper

<Card horizontal title="CLI API Reference" icon="terminal" href="/api-reference/cli/new">
  Detailed documentation for all CLI commands and options.
</Card>
