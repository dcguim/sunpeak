---
title: "Inspector"
description: "Inspect MCP Apps in replicated ChatGPT and Claude runtimes."
keywords: ["MCP Apps", "ChatGPT Apps", "Claude Apps", "MCP app testing", "ChatGPT app testing", "inspector", "local development"]
---

import InspectorSnippet from '/snippets/inspector.mdx';

<img
  src="https://cdn.sunpeak.ai/chatgpt-simulator.png"
  alt="MCP App Inspector"
/>

## Overview

The inspector replicates MCP App host runtimes (ChatGPT, Claude) for local development and testing. Switch between hosts from the sidebar or via the `host` URL parameter.

There are two ways to use the inspector:

<Tabs>
  <Tab title="Standalone (any MCP server)">
    Inspect any MCP server — no sunpeak project required:

    ```bash
    sunpeak inspect --server http://localhost:8000/mcp
    sunpeak inspect --server "python my_server.py"
    ```

    See [sunpeak inspect CLI Reference](/api-reference/cli/inspect) for all options.
  </Tab>
  <Tab title="Built into sunpeak dev">
    In sunpeak projects, the inspector is built into the dev server:

    ```bash
    sunpeak dev
    ```
  </Tab>
</Tabs>

## Why Use the Inspector?

<Card horizontal title="Instant Feedback" icon="rotate">
  See changes immediately with hot module replacement. No more cache issues or constantly reloading the host.
</Card>
<Card horizontal title="Visualize Design Variations" icon="palette">
  Toggle between light/dark themes, mobile/tablet/desktop views, and other platform states.
</Card>
<Card horizontal title="Testing" icon="flask">
  Perform manual and automated tests locally. Create CI/CD integration and regression tests.
</Card>
<Card horizontal title="No Deployment Needed" icon="desktop">
  Develop locally without setting up MCP servers.
</Card>

## Simulations

Simulations define test scenarios for your resources — combining tool definitions, mock data, and platform state.

Simulations are **JSON files** in a flat `tests/simulations/` directory. Each simulation references a tool file by name.

```json
// tests/simulations/show-example.json
{
  "tool": "show-example",
  "userMessage": "Show me an example",
  "toolInput": { "query": "example" },
  "toolResult": {
    "structuredContent": { "items": [] }
  }
}
```

Tool metadata (name, description, schema, annotations) lives in `src/tools/show-example.ts`, not in the simulation. The resource component and metadata live in `src/resources/example/example.tsx` and are linked via the tool file's `resource: 'example'` field.

See [Simulations API Reference](/api-reference/simulations/simulation) for the complete JSON schema and conventions.


Simulations can be passed into:

<AccordionGroup>
  <Accordion title="Inspector - for local testing">
    The `Inspector` component provides a local development environment that replicates host runtimes (ChatGPT, Claude).

    <InspectorSnippet />

    [View Inspector API Reference](/api-reference/simulations/inspector)
  </Accordion>

  <Accordion title="runMCPServer - for host testing">
    The `runMCPServer` function serves your simulations to hosts like ChatGPT via the Model Context Protocol.

    ```bash
    sunpeak dev
    ```

    See [runMCPServer API Reference](/api-reference/simulations/run-mcp-server) for configuration options and connecting to a host.


    [View MCP Server API Reference](/api-reference/simulations/run-mcp-server)
  </Accordion>
</AccordionGroup>

## Dive Deeper

<Card horizontal title="sunpeak inspect" icon="terminal" href="/api-reference/cli/inspect">
  Inspect any MCP server (standalone, no project needed).
</Card>
<Card horizontal title="Simulations API Reference" icon="folder" href="/api-reference/simulations/simulation">
  JSON schema, conventions, and auto-discovery.
</Card>
<Card horizontal title="Inspector API Reference" icon="desktop" href="/api-reference/simulations/inspector">
  Full component documentation and configuration options.
</Card>
